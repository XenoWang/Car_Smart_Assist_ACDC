# =============================================================================
# 开发与可选工具 —— 全部可以走国内镜像源
# =============================================================================
# 安装：
#     pip install -r requirements/requirements-dev.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 这份文件不是运行项目所必需的，只在开发、测试、导出部署时用。
# 拆分理由：运行时依赖要精简（简历项目讲部署时会问镜像体积），
# 开发工具不该混进运行时。
# =============================================================================

# --- 测试 ---
pytest>=8.2
pytest-cov>=5.0                # 覆盖率；报告里放一张覆盖率截图比空口说「有测试」强
pytest-xdist>=3.6              # 并行跑测试

# --- 代码质量 ---
ruff>=0.6                      # lint + format，二合一，配置见 pyproject.toml
mypy>=1.11                     # 类型检查；核心模块（geometry / policy / schema）要求全注解
pre-commit>=3.7                # 提交前钩子，配置见 .pre-commit-config.yaml

# --- 实验与 Notebook ---
jupyterlab>=4.2
ipykernel>=6.29                # 把本项目的 .venv 注册成 notebook 内核：
                               #   python -m ipykernel install --user --name car-smart-assist

# --- 可选：模型导出（只有跑 scripts/export_onnx.py 时才需要）---
# onnxruntime-gpu 的 CUDA 版本必须和对齐 torch 的 CUDA（本项目是 12.x）。
# 装之前先确认版本匹配，装错版本会让导出校验的数值对不上，很容易误判成模型 bug。
onnx>=1.16
onnxruntime-gpu>=1.20

# --- 可选：超参搜索（先不用，预留）---
# optuna>=3.6
