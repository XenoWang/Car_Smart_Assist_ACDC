"""检查点保存与加载。

职责:
    - 保存权重 + 优化器 + scheduler + epoch/step + 配置快照 + git commit hash
    - 原子写入（先写临时文件再 rename），避免训练中断产生半个文件
    - 提供 best/last 两套策略与按指标取最优的加载逻辑
    - 加载时校验配置兼容性，不兼容要显式报错而不是静默错配

备注: 存配置快照是为了让每次实验可复现 —— 简历项目里这点很加分
"""
