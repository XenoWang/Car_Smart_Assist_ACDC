"""IO 工具。

职责:
    - 读写 json / yaml / jsonl，统一 UTF-8 编码（Windows 下默认编码会出问题，必须显式指定）
    - 原子写入与安全读取（不存在时的明确报错）
    - 校验和计算（下载数据时用）
    - 路径工具：项目根定位、相对路径转绝对路径

备注: 本项目在 Windows 上开发，编码与路径分隔符的坑集中在这里解决
"""
