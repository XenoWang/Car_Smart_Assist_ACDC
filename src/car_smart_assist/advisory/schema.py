"""Stage 1 -> Stage 2 之间传递的结构化数据契约。

职责:
    - 定义 PerceptionResult：路况、接管边界与置信度、各目标的方向/距离/不确定度
    - 定义 AdvisoryResult：是否接管、风险等级、给司机的文本、以及触发它的规则/证据
    - 两个 Stage 只通过这个 contract 耦合；改字段要同步改两侧和评测脚本

备注: 这一层是整个项目可解释性的落点 —— 每条建议都能追溯到具体的结构化证据
"""
