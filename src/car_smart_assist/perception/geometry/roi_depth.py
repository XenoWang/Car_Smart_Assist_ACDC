"""把深度图 / 点云转成「目标框级距离」标签。

职责:
    - 在检测框内取稳健统计量（中位数 / 分位数裁剪）作为该目标的距离，避免背景点污染
    - 处理遮挡、截断框、过小框等无效情形，返回 valid 标志而不是硬凑一个数
    - 提供框到自车距离与框到相机距离的口径换算（两者差一个自车偏移，容易搞错）

依赖: perception.geometry.pinhole
被谁调用: scripts/build_distance_labels.py
备注: 距离口径必须在 docs/label_spec.md 里写死，训练和评测用同一个
"""
