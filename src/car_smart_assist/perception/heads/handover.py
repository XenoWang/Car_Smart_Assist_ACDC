"""接管边界判断头（本项目的核心任务）。

职责:
    - 在共享特征上做分类：当前帧是否已经越过「应该交还控制权」的边界
    - 建议做成有序分级（如 confidence_levels）而不是纯二分类，便于输出「接近边界」的预警
    - 输出必须带置信度，供 Stage 2 的策略层决定说不说、说多强

依赖: 共享骨干特征
备注: 这是自己构造的标签，定义见 docs/handover_policy.md —— 标签口径是本项目最需要说清楚的地方
"""
