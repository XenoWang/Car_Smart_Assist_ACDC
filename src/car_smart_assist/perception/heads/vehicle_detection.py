"""车辆检测头（前车 / 来车）。

职责:
    - 检测画面中的车辆并区分「同向（前车）」与「对向（来车）」，这是距离解释的前提
    - 输出 boxes + scores + 方向类别；轻量实现可先用 anchor-free 单尺度
    - 需要与 distance head 对齐框的索引，保证「哪个框对应哪个距离」不出错

依赖: 共享骨干特征
备注:
    勘误（2026-09-21）—— 本文件早期版本写「ACDC 无检测标注，检测监督来自外部数据集」，
    这是错的。ACDC v2 提供 gt_detection_trainval.zip（2006 张图的检测框标注）。
    检测监督改用 ACDC 自身，不再跨域。

    ⚠️ 但 ACDC 的检测标注**不区分「前车 / 来车」**，方向必须自己推导，
    这是原设计漏掉的一步。可选路径：
      (a) 用 3D 朝向角（KITTI 的 rotation_y 可直接用；ACDC 是否有 3D 标注待确认）
      (b) 只用 2D：目标框横向位置 + 车道几何/消失点推断同向还是对向
    推导置信度低的目标必须标为 unknown 并排除出方向损失，
    宁可不说方向，也不要给错方向 —— 前车与来车的处理建议完全相反。
"""
