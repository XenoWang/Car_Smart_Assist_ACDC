"""车辆检测头（前车 / 来车）。

职责:
    - 检测画面中的车辆并区分「同向（前车）」与「对向（来车）」，这是距离解释的前提
    - 输出 boxes + scores + 方向类别；轻量实现可先用 anchor-free 单尺度
    - 需要与 distance head 对齐框的索引，保证「哪个框对应哪个距离」不出错

依赖: 共享骨干特征
备注: ACDC 无检测标注，检测监督同样来自外部数据集，与距离标签同源，注意域差异
"""
