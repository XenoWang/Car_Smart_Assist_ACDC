# 项目路线图

> 写这份路线图的目的：让项目推进有节奏、可交付、可中断。
> 每个阶段结束时都应该有一个**能拿给人看的东西**，
> 避免出现「做了三个月但说不出做完了什么」的情况。

---

## 里程碑总览

| 版本 | 主题 | 交付物 | 状态 |
|------|------|--------|------|
| v0.0.1 | 项目骨架 | 目录结构、配置体系、依赖、文档框架 | ✅ 已完成 |
| v0.1.0 | 数据管线 | 预处理可用、统计报告、距离标签口径定稿 | ⬜ 未开始 |
| v0.2.0 | Stage 1 感知 | 多任务模型跑通，三组指标有基线 | ⬜ 未开始 |
| v0.3.0 | 接管边界 | 标签体系定稿 + 人工校验一致率 + 分类器指标 | ⬜ 未开始 |
| v0.4.0 | Stage 2 建议 | 结构化输出 → 司机提示生成链路可用 | ⬜ 未开始 |
| v0.5.0 | 评测报告 | 端到端评测 + 失败案例分析 | ⬜ 未开始 |
| v1.0.0 | 收尾 | 文档完整、指标可复现、demo 可演示 | ⬜ 未开始 |
| v1.1.0 | 时序扩展（可选） | 引入帧间状态，支持相对速度与 TTC | ⬜ 未开始 |

---

## v0.1.0 — 数据管线

**目标**：数据能喂进模型，且口径清楚。

- [x] 下载并校验 KITTI（S3 直链，免注册）—— 7481 训练帧已验证
- [ ] 注册申请并下载 ACDC 四个包，校验 md5，解压到 `data/raw/acdc/`
- [ ] **解压后确认 ACDC 检测标注的实际格式**（类别清单、是否含 3D 信息）
      —— 在此之前不要假定它与 KITTI 类别体系一致
- [ ] 实现 `data/label_mapping.py`，映射到 Cityscapes 19 类
- [ ] 按 scene 切分 train/val/test，落盘 `split.json`
- [ ] 实现 `acdc_seg.py`，分割任务可独立跑通
- [ ] **实现「前车 / 来车」方向推导**（ACDC 与 KITTI 都不提供，见 `label_spec.md` 4.1）
- [ ] **实现 KITTI `DontCare` 的排除逻辑**（占目标数 26%，当背景用会压低召回率）
- [ ] 实现 `perception/geometry/pinhole.py` 与 `roi_depth.py`，含单元测试
      —— 注意 ACDC(1920×1080) 与 KITTI(1224×370) 长宽比不同，内参不能共用一套
- [ ] 实现 `build_distance_labels.py`，产出带 `valid` 标志的距离标签
- [ ] 实现接管边界标签构造，口径写死到 `docs/handover_policy.md`
- [ ] 跑 `analyze_dataset.py`，产出统计与抽样图

**验收标准**：`tests/unit/test_geometry.py` 全绿；
统计数据能回答「四个天气子集各多少张、距离标签覆盖多少帧、接管边界三档各占多少」。

**风险**：距离标签的构造是整个项目最大的不确定性。
如果 KITTI 与 ACDC 的域差异大到无法接受，需要在这里就决定是否
退回「只做分割 + 路况分类」的简化版本。**不要拖到训练之后才发现。**

> 已提前消解的一项风险：ACDC v2 自带检测框，检测不必再跨域。
> 域差异现在只影响「距离」这一个输出维度，而不是「目标在哪 + 有多远」两项。

---

## v0.2.0 — Stage 1 感知

**目标**：多任务模型跑通，每个任务都有可报告的基线指标。

- [ ] 实现 `perception/backbones/segformer.py`，权重能正常加载
- [ ] 实现四个 head 与 `multitask.py`
- [ ] 实现 `engine/` 全套（trainer / optimizer / scheduler / checkpoint / callbacks）
- [ ] 实现分割损失与分类损失，跑通单任务训练
- [ ] 加多任务联合训练，处理 loss 权重
- [ ] 实现 `evaluation/metrics/` 下四组指标
- [ ] 训练出第一版 checkpoint，产出完整指标

**验收标准**：
- 分割 mIoU 在 val 上达到可用水平（MiT-B1 在 ACDC 上应能到 60+ mIoU）
- 路况分类 macro F1 > 0.9（这个任务标签质量最好，做不到说明流程有问题）
- 训练可复现：同 seed 跑两次，指标差异在合理范围内

**注意**：路况分类是个「太简单」的任务，指标很容易很好看。
在报告里要如实说明它在整个系统中的作用有限 ——
主动承认比被人指出强。

---

## v0.3.0 — 接管边界

**目标**：把项目的核心原创点做扎实。

- [ ] 阈值标定：用 val 集上的 PR 曲线确定 T1 / T2
- [ ] 训练接管边界头，报告三档的混淆矩阵
- [ ] **人工标注 300–500 帧作为校验集**，计算与规则标签的一致率（Cohen's kappa）
- [ ] 分析规则标签与人工判断不一致的案例，写进报告
- [ ] 给出「纯规则 baseline」与「学习模型」的对比

**验收标准**：
- 等级 2 的召回率 > 0.90（安全优先，recall 优先于 precision）
- 报告中如实给出 kappa 值，并讨论规则在哪些场景失效
- 有明确的「本系统不声称覆盖的场景」清单

**这是简历里最该展开讲的部分** —— 因为它体现了「自己定义问题并诚实评估」的能力，
比「用了什么模型」有价值得多。

---

## v0.4.0 — Stage 2 建议生成

**目标**：结构化感知结果 → 自然语言司机提示。

- [ ] 实现 `advisory/schema.py` 契约
- [ ] 实现 `policy/handover_rules.py` 与 `risk.py`，含单元测试
- [ ] 实现 `prompt/` 模板，保证与标签构造一致
- [ ] 实现 `llm/backend.py` 抽象与 `template` 兜底实现
- [ ] 先跑 **纯模板 baseline** 并评测（这一步必须有）
- [ ] 构造指令数据集，含 ≥40% 负样本
- [ ] QLoRA 微调 VLM，与模板 baseline 对比
- [ ] 实现生成后处理：禁词、长度、要素校验、降级路径

**验收标准**：
- 模板 baseline 的指标先立起来
- 微调后相对 baseline 有可度量的改进，或如实说明没有改进及原因
- 漏报率与误报率同时报告，不允许只报一个

**风险**：如果微调没有超过模板 baseline，这本身是个有价值的结论
（说明这个任务不需要大模型），要在报告里正面写出来，而不是删掉这部分。

---

## v0.5.0 — 评测与报告

- [ ] `evaluate.py` 支持 `--stage all`，产出完整报告
- [ ] 分天气子集的指标表
- [ ] 距离分桶指标 + 误差-距离曲线
- [ ] 接管边界的 PR 曲线与阈值扫描
- [ ] 失败案例导出与拼图
- [ ] 定性样例拼图（好例子也要有）
- [ ] 性能计时：预处理 / 感知 / 生成分开报告

---

## v1.0.0 — 收尾

- [ ] README 补齐：架构图、指标表、快速开始、复现步骤
- [ ] 所有文档与实际代码一致（对不上就以代码为准改文档）
- [ ] 测试覆盖率报告
- [ ] demo：单图或短视频端到端演示
- [ ] 明确列出已知局限

---

## 明确不做的事

写下来是为了防止范围蔓延 —— 这类项目失败的原因，
十次里有八次是「什么都想做一点，结果什么都没做扎实」。

- ❌ 不做真实的车辆控制（本项目只输出提示，不输出控制指令）
- ❌ 不做多传感器融合（只有单目相机）
- ❌ 不做时序建模（v1.1 才考虑）
- ❌ 不追求 SOTA 指标（受限于 12GB 显存和单卡）
- ❌ 不声称达到任何功能安全等级（不做 ISO 26262 相关工作）
